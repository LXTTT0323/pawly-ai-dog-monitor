export const dynamic = "force-dynamic";
export default function OwnerAccessPage() {
  // Kept only for bookmarks from the private-key beta. Platform sign-in now owns access.
  return <meta httpEquiv="refresh" content="0; url=/setup" />;
}
